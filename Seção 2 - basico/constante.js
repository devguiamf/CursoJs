/*  
    Nao podemos criar constantes como palavras reservadas
    constantes precisam ter nomes significativos
    não podemos começar o nome de uma constante com numero
    não podem conter espaços
    utilizamos camelcase
    uma constante é uma case-sensitive
    não podemos modificar o valor de uma constante
    não utilizar var, utilize const
 */


const primeiroNumero = 5;
const segundoNumero = 10;

const resultado = primeiroNumero * segundoNumero
console.log(resultado);

console.log(typeof resultado); // como descobrir a tiapagem da variavel 